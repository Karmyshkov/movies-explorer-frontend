import { SearchForm } from "../SearchForm";
import { FilterCheckbox } from "../FilterCheckbox";

export const Movies = () => {
  return (
    <>
      <SearchForm />
      <FilterCheckbox />
    </>
  );
};
